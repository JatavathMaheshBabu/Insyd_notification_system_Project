# Frontend — Insyd Notifications POC

React (Vite) app that lets you trigger events and view notifications via polling.

## Env

Copy `.env.example` to `.env` and set API URL if needed:

```env
# Defaults to http://localhost:4000 if not set
VITE_API_URL=http://localhost:4000
```

## Scripts

```bash
npm run dev
npm run build
npm run preview
```

